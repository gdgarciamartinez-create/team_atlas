import { defineConfig } from 'vite'
import react from '@vitejs/plugin-react'

// ❌ UI LEGACY (Scanner) ELIMINADA — TEAM ATLAS ONLY
throw new Error("❌ UI LEGACY (Scanner) ELIMINADA — TEAM ATLAS ONLY");

export default defineConfig({
  plugins: [react()],
})
